apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-default-deny-ingress
  namespace: manufacturing-monitoring
spec:
  # manufacturing-monitoring 안의 모든 Pod 에 대해 ingress 를 기본 차단한다.
  podSelector: {}
  policyTypes:
    - Ingress
